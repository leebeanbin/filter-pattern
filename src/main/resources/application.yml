server:
  port: 8080

spring:
  application:
    name: filter-pattern
  datasource:
    url: jdbc:mariadb://localhost:3306/product
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true